<?php

namespace Tests\{{ context.namespacePrefix }}\{{ context.srcDir | capitalize }}{% if context.namespace %}\{{ context.namespace }}{% endif %};

class {{ context.name }}{{ context.nameSuffix }}Test extends \Generated\Tests\{{ context.namespacePrefix }}\{{ context.srcDir | capitalize }}{% if context.namespace %}\{{ context.namespace }}{% endif %}\{{ context.name }}{{ context.nameSuffix }}Test
{
    public function getClassInstance()
    {
        throw new \Exception('"getClassInstance" is not implemented.');
    }

{% for step in steps if step.type in ['validator', 'formatter'] %}
    /**
     * @return array
     */
    public function {{ step.functionName }}DataProvider()
    {
        throw new \Exception('{{ step.functionName }}DataProvider is not implemented.');
    }

{% endfor %}
}