<?php

namespace {{ context.namespace }};

class {{ context.className }} extends \Generated\{{ context.namespace }}\{{ context.className }}
{
{% for step in context.stepHeaders if step.type in ['validator', 'formatter'] %}
    public function {{ step.functionName }}({{ method_arguments(step.arguments) }})
    {
        throw new \Exception('"{{ step.functionName }}" is not implemented.');
    }
    
{% endfor %}
{% for step in context.stepHeaders if step.type == 'call' and step.service is null %}
    protected function {{ step.functionName }}({{ method_arguments(step.arguments) }})
    {
        throw new \Exception('"{{ step.functionName }}" is not implemented.');
    }

{% endfor %}
}
