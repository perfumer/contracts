<?php

namespace {{ class_builder.namespace }};

class {{ class_builder.className }} extends \Generated\{{ class_builder.namespace }}\{{ class_builder.className }}
{
{% for method in class_builder.methods %}
{% if method.isAbstract %}
    protected function {{ method.name }}({% for argument in method.arguments %}${{ argument }}{% if not loop.last %}, {% endif %}{% endfor %})
    {
        throw new \Exception('Method "{{ method.name }}" is not implemented yet.');
    }
{% endif %}
{% endfor %}
}
